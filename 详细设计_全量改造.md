# 详细设计：思绪思维导图 全量改造（v1.0.2 → v1.0.17）

> 作者：CodeBuddy
> 日期：2026-08-29
> 范围：多文件工作区 + 多 Sheet、自定义标题栏、节点备注代码高亮、.emmx 导入、画布背景、透明度、快捷键、macOS 毛玻璃、NSIS 安装包
> 说明：本文档同时承担「设计说明」与「代码审查（Bug 清单）」两份职责。Bug 项标注 ✅ 为笔者已亲自核验源码，其余由探查 agent 报告并经核对。

---

## 0. 系统总览

```
┌──────────────────────────────────────────────────────────────┐
│ 自定义标题栏 FileTabs.vue (34px, -webkit-app-region:drag)      │
├──────────┬───────────────────────────────────────┬───────────┤
│ Sidebar  │  mindMapContainer (simple-mind-map)    │ Navigator │
│ 触发按钮  │   - 画布                              │ 工具栏     │
│ (可透明)  │   - 背景(纯色/图片,localConfig)        │ (可透明)   │
│           │   - 节点备注弹窗(NodeNote)            │            │
├──────────┴───────────────────────────────────────┴───────────┤
│ SheetTabs.vue (40px, 工作表切换)                              │
└──────────────────────────────────────────────────────────────┘
        │ 状态/数据
        ▼
workbookState.js（多文件状态机） → api/index.js → localStorage + .smm 文件
        ▲
        │ IPC / 文件
electron-app/main.js（主进程: 端口/安装/文件关联） → make_installer.nsi（NSIS 出包）
```

---

## 1. 多文件工作区 + 多 Sheet（状态机重构）

### 1.1 实现
- `web/src/api/workbookState.js`：纯状态机，每个 workbook 独立持有 `sheetState`，从根上隔离「切换文件数据串」问题。
- `web/src/api/index.js`：读写为当前激活 workbook 的 sheetState 委托。
- `web/src/pages/Edit/Index.vue`：FileTabs 事件 → `switchWorkbook/closeWorkbook/renameWorkbook`，顺序为 `before-workbook-switch`(先存)→`switchWorkbook`→`workbook-switched`(再载)。
- `FileTabs.vue` / `SheetTabs.vue`：标签 UI、重命名、关闭、窗口控制。

### 1.2 已修历史坑（确认）
数据串（状态机隔离）、保存错文件（`doSave` 用 `getCurrentFilePath()`）、防重复打开（`findByPath` 去重）、另存为刷新（`workbook-list-changed` + `applySaveAs`）、拖拽覆盖（走新建不覆盖）、重命名同步磁盘（`smmApi.renameFile`）。

### 1.3 Bug
| 严重度 | 位置 | 现象 | 修复 |
|---|---|---|---|
| 中 ✅ | `Edit.vue:414` `storeData({ root: data })` | 把完整数据又包一层 `root`，形成 `activeSheet.data.root=<整份数据>`。被 `manualSave()` 用 `storeData(this.mindMap.getData(true))` 掩盖，目前不落盘损坏，但 `data_change` 自动保存分支逻辑错误、脆弱。 | 改为 `storeData(data)` 与 `manualSave` 一致 |
| 低 | `api/index.js:95` `if ('exceeded')` | 字符串常量恒真，任何 `storeData` 抛错都误报"本地存储超限" | 判断 `error.name === 'QuotaExceededError'` |
| 低 | `api/index.js:32` 双写 localStorage | `saveSheetState` 写 `SIMPLE_MIND_MAP_SHEETS`，`storeData` 写 `SIMPLE_MIND_MAP_DATA`，冗余 | 收敛为单写 |
| 低 | `workbookState.js:182` `skipOldWritebook` | 形参接收但从未使用 | 删除或补实现 |

---

## 2. 自定义标题栏

### 2.1 实现
- 标题栏即 `FileTabs.vue`（顶部 34px，`-webkit-app-region: drag`），窗口按钮走 `window.smmApi.windowControls`（minimize/maximize/close）。
- `main.js` 窗口 `frame:false` + `thickFrame:false`；`smm:window-maximize` 为切换最大化/还原。

### 2.2 Bug
| 严重度 | 位置 | 现象 | 修复 |
|---|---|---|---|
| 中 ✅ | `Edit.vue:1560-1592` | `.editContainer{top:0}`、`.mindMapContainer{top:0; bottom:40px}`。底部为 SheetTabs 留了 40px，但**顶部无 34px 偏移**。固定 FileTabs(34px) 叠在画布上，画布顶部 34px 被 drag 区捕获指针 → 该区域无法点击/框选（根节点居中时尤明显）。 | `mindMapContainer` 与主体加 `top:34px`（或 `padding-top:34px`），与底部对称 |
| 低 | `FileTabs.vue:2` `@dblclick.self` | 双击最大化仅在空白条触发，双击品牌/标签区不触发 | 改为事件冒泡或整体绑定 |

---

## 3. 节点备注代码高亮

（详见《详细设计_节点备注代码高亮.md》，此处摘要）

### 3.1 实现
`NodeNote.vue` 内嵌 ToastUI Editor（3.2.2）+ `@toast-ui/editor-plugin-code-syntax-highlight` + `prismjs`（16 语言，显式 import 语言包 + `prism.css` 主题）。编辑器单例，确认时 `node.setNote(editor.getMarkdown())`，存于节点 `note` 字段。

### 3.2 Bug
| 严重度 | 位置 | 现象 | 修复 |
|---|---|---|---|
| 中低 | `NodeNote.vue:152` `insertCodeBlock` | 固定写 `mdEditor`；用户切到 WYSIWYG 标签时插入内容进隐藏 MD 源码、预览不刷新 | 用 `editor.getEditor()` 取活动编辑器 |
| 低 | `NodeNote.vue:111` `handleNodeActive` | 编辑中若激活节点变化，工具栏「备注」入口会写错节点（被 modal 缓解） | 打开时快照目标节点，确认固定写入 |
| 隐患 | `NodeNote.vue:60` `prism-xml-doc` | `codeLangs` 的 `xml` 实际靠 `prism-markup` 别名着色，显式导入 `prism-xml-doc` 冗余且易误导 | 删除该导入或改选项值为 `markup` |
| UX | `insertCodeBlock` | 插入 ` ```python\n\n``` ` 后光标在块外 | `replaceSelection` 后 `setSelection` 落入两 fence 间 |
| 一致 | 视觉 | 弹窗仍是 Element/ToastUI 默认白底，深色画布下刺眼；未套 macOS 毛玻璃 | 加 glass 样式 + Prism 主题随明暗切换 |

---

## 4. .emmx 导入（亿图脑图/MindMaster 逆向）

### 4.1 实现
- `web/src/utils/parseEmmx.js`：先 `document.xml` 标准解析；否则遍历 `mmpage/page*.bin` 私有二进制，靠「父指针 + 子列表」还原任意深度层级，含 varint 解析、UTF-8 替换字符校验、防环、多顶层取最大子树、兜底提示页。
- `Import.vue:305-337` `handleEmmx` 用 `try/catch` 包裹，损坏文件由 JSZip 抛错被捕获 → 弹错误提示，白屏风险已堵。

### 4.2 Bug
| 严重度 | 位置 | 现象 | 修复 |
|---|---|---|---|
| 中 | `parseEmmx.js:409-411` 正则 `/^mmpage\/page.*\.bin$/` `/^page.*\.bin$/` | 只匹配根目录或 `mmpage/` 直接子级；若页存为 `Document/page.bin`、`content/page.bin` 等嵌套目录，**完全匹配不到 → 该页被静默跳过**，导入残缺 | `zip.filter(p => /(^|\/)page.*\.bin$/i.test(p))` |
| 低/中 | `parseEmmx.js:211` `if (text.length > 1000) continue;` | 把"解码文本长度>1000"当误匹配过滤，合法长话题被**静默丢弃** | 移除长度阈值（误匹配已由 `\ufffd` 拦截） |

---

## 5. 画布背景设置

### 5.1 实现
`Setting.vue` → `applyCanvasBackground` 写 `mindMap.el` style(`!important`) 并 `setLocalConfig({canvasBackground})`；`Edit.vue:1116-1158` `applyStoredCanvasBackground` + `onBgRenderEnd`（`node_tree_render_end` 兜底）重应用。背景存 `localConfig`，**不写 .smm**，故不串文件。已验证兜底事件确实触发。

### 5.2 Bug
| 严重度 | 位置 | 现象 | 修复 |
|---|---|---|---|
| 低 | `Edit.vue:1136-1145` | `background-image:url(...)` 直接设置，失效的 `file://`/dataURL 仅回退纯色 `#fff`，无错误提示 | 预加载 `new Image()`，失败回退纯色并轻提示 |

---

## 6. 透明度可调

### 6.1 实现
`Setting.vue` 四个滑块 `toolbarOpacity/sidebarOpacity/navigatorOpacity/fileTabsOpacity` → `updateLocalConfig` → vuex 订阅里 `storeLocalConfig` 持久化（重启可恢复）。

### 6.2 Bug
| 严重度 | 位置 | 现象 | 修复 |
|---|---|---|---|
| 中 ✅ | `Sidebar.vue` `sidebarContainer` | `sidebarOpacity` 仅被 `SidebarTrigger.vue:6` 读取；实际左侧面板 `Sidebar.vue`（`.sidebarContainer`）**完全未应用该值** → 调"左侧工具栏透明度"只让触发按钮变透明，主面板无视 | 在 `Sidebar.vue` 根容器加 `:style="{opacity: sidebarOpacity ?? 1}"` 并 `mapState` 取值 |
| 低 | `SheetTabs.vue:2` | 底部工作表栏误用顶部 `fileTabsOpacity`，无独立底部设置 | 命名澄清或补独立项 |

（其余 `toolbarOpacity→Toolbar.vue`、`navigatorOpacity→NavigatorToolbar.vue`、`fileTabsOpacity→FileTabs.vue` 绑定正确。）

---

## 7. 快捷键

### 7.1 实现
`Edit.vue:539-575` `onGlobalKeydown` 处理 Ctrl/Cmd+S（保存）、Ctrl+Shift+S（另存为）、Ctrl+O（打开）、F2（编辑当前节点）；`mounted` 注册 `window.addEventListener('keydown')`，`beforeDestroy` 解绑。原先"声明但未注册"坑已修，desktop 端生效。

### 7.2 Bug
| 严重度 | 位置 | 现象 | 修复 |
|---|---|---|---|
| 中 | `Edit.vue:539` `onGlobalKeydown` | 未检查 `document.activeElement`；在 `<input>/<textarea>/contenteditable`（标签重命名框、搜索框、节点备注）中按 **F2** 会误触发编辑画布节点，按 **Ctrl+O** 弹打开框并 `preventDefault` 阻断输入 | 方法开头加：`const t=document.activeElement; if(t&&(t.tagName==='INPUT'||t.tagName==='TEXTAREA'||t.isContentEditable)) return` |
| 低 | 与 simple-mind-map 自带 KeyboardNavigation 可能双触发 F2 | 叠加无害但冗余 | 视情况收敛 |

---

## 8. macOS 毛玻璃视觉

### 8.1 实现
`web/src/styles/macos.less`：全局设计系统（变量 + Element UI 组件覆盖：按钮/输入/下拉/弹窗/消息/滑块/表格），浅色 `:root` 与 `body.isDark` 双套。由 `App.vue` `@import` 全局引入。毛玻璃 `backdrop-filter` + 半透明 `background-color` 写法正确。

### 8.2 Bug / 不一致
| 严重度 | 位置 | 现象 | 修复 |
|---|---|---|---|
| 低 | `macos.less` | 未使用变量（死代码）：`--macos-accent-2`、`--macos-radius-xl`、`--macos-bg-glass-soft` 0 引用；`--macos-surface-3` 仅 1 处 | 清理或补使用 |
| 说明 | 第三方编辑器 | ToastUI/Markdown 等第三方编辑器内部元素未纳入覆盖，不获得毛玻璃/配色 | 若用此类组件需另加 selector |

---

## 9. 主进程 main.js + NSIS 安装包

### 9.1 实现
- `main.js`：静态服务器 `tryListen` 端口回退（51888→51898）；`--install` 模式 `win.loadFile(install.html)` 走 `file://`；`smm:save-workbook/write-file/rename-file/open-workbook/import-file` 等 IPC 完备；`doInstall` 拷贝程序、建快捷方式、写卸载、关 server、可选启动。
- `make_installer.nsi`：NSIS 脚本，含自动卸载旧版、拷贝 `win-unpacked`、卸载注册表、桌面/开始菜单快捷方式。
- 运行时文件：`win-unpacked` 内 `icudtl.dat/*.pak/d3dcompiler_47.dll/ffmpeg.dll` 齐全（"缺 icudtl.dat 打不开"已解决）。

### 9.2 Bug
| 严重度 | 位置 | 现象 | 修复 |
|---|---|---|---|
| 高 ✅ | `main.js:212` `APP_EXE='MindMap.exe'` vs `package.json:16` `productName:'思绪思维导图'` | 实际产物 exe 为 `思绪思维导图.exe`；`doInstall` 据此建桌面/开始菜单快捷方式（main.js:390-408）与卸载图标（337）。结果：**经内置安装器装出的程序，快捷方式指向不存在的 `MindMap.exe` → 点不开** | `APP_EXE = app.getName() + '.exe'`（即 `思绪思维导图.exe`） |
| 中 | `main.js` 全文件 | 无 `app.on('second-instance')`/`process.argv` 解析/`setAsDefaultProtocolClient`，NSIS 也未注册 `.smm` → **双击 .smm 用本程序打开未接通**，`openWorkbook` 仅指"打开对话框" | 补 `second-instance` + 启动 argv 解析 + 渲染端复用 `handle_file_url` |
| 低 | `make_installer.nsi:7,39` | 硬编码绝对路径 `E:\03_学习文件\mind-map-main\...`，换机器无法打包 | 改为相对路径/变量 |
| 低 | `make_installer.nsi:13` `VERSION "1.0.17"` | 与 git 最新 `v1.0.16` 不一致，且每次发版需手改 | 由 package.json 注入 |
| 低 | 注册表键 | NSIS 用 `com.mindmap.app`，内置安装器用 `MindMap` → 两套安装器互不可见，覆盖安装残留 | 统一 appId/键/exe 名 |
| 低 | `main.js:582` | `if (attempt > 10)` 实际试 11 个端口，错误文案写 `startPort + 10`，差 1 | 文案改 `+ 11` 或循环值 |

---

## 10. 优先级与修复路线

**P0（立即修）**
1. 模块 9 高：内置安装器 exe 名错误 → 快捷方式打不开（影响交付）。

**P1（本迭代修）**
2. 模块 1 中：`storeData({root:data})` 双重嵌套（潜伏数据风险）。
3. 模块 2 中：标题栏遮挡画布顶部 34px。
4. 模块 3 中：bin 正则漏嵌套目录 → 导入残缺。
5. 模块 5 中：侧栏透明度不生效。
6. 模块 6 中：输入框聚焦误触发快捷键（尤其 F2/Ctrl+O）。
7. 模块 9 中：文件关联缺失（若需求含双击打开）。

**P2（可排期）**
8. 模块 3 低：长文本话题被误删。
9. 模块 4 低：背景图加载失败无反馈。
10. 模块 7 低：未用 CSS 变量清理。
11. 模块 8 低：模块化冗余（详见各模块低危项）。

---

## 11. 验收标准（全量）

- [ ] 多文件切换/关闭/重命名后数据不串、保存路径正确、磁盘文件同步。
- [ ] 标题栏不遮挡画布；双击空白区最大化；三按钮正常。
- [ ] 节点备注代码块按语言高亮；插入按钮在任意编辑器模式下正确工作。
- [ ] .emmx 嵌套目录页不丢失；长文本话题不误删。
- [ ] 画布背景（纯色/图片）切换/重开不丢失；图片失效有反馈。
- [ ] 四个透明度滑块均真实作用于对应元素，重启保留。
- [ ] 在输入框中按 F2/Ctrl+O 不误触发全局快捷键。
- [ ] macOS 风格视觉统一；无死 CSS 变量。
- [ ] 安装包快捷方式可正常打开应用；双击 .smm 可打开（若需求含）。
```
