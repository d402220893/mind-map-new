# 测试框架设计（思绪思维导图）

> 配套：《详细设计_全量改造.md》《功能点细化与代码架构.md》
> 作者：CodeBuddy ｜ 日期：2026-08-29
> 核心约束：**测试代码不参与打包**（生产安装包 `Setup.exe` 不含任何 `*.test.*` 与 `tests/`）。

---

## 1. 框架选型与理由

| 方案 | 评估 | 结论 |
|---|---|---|
| Vitest / Jest | 需引入大量依赖，且与本项目 Vue CLI + webpack4 集成成本高、易冲突 | ✗ 弃用 |
| **Node 原生测试运行器**（`node --test` + `node:assert`） | 零依赖、Node≥18 内置；纯逻辑模块直接用，Vue SFC 通过「抽纯函数」间接覆盖 | ✓ **采用** |

- 共识：项目已有 `web/src/api/workbookState.test.mjs` 采用手写零依赖 harness，本框架延续该风格并升级为官方 `node:test`。
- 测试文件为 `.mjs`（ESM），仅被 Node 测试运行器消费，**不被 webpack 入口 import**，因此天然不会进入前端 bundle。

---

## 2. 目录结构

```
web/
├─ src/
│  ├─ api/
│  │  ├─ workbookState.js          # 纯状态机（被测）
│  │  ├─ storageErrors.js          # 新增：配额异常判定（被测 + 接入 api/index.js）
│  │  └─ index.js                  # 存储委托（接入 storageErrors）
│  ├─ utils/
│  │  ├─ parseEmmx.js              # .emmx 解析（被测；新增 isPageBinEntry）
│  │  └─ shortcutGuard.js          # 新增：快捷键焦点守卫（被测 + 接入 Edit.vue）
│  └─ pages/Edit/components/
│     ├─ Edit.vue                  # 接入 shortcutGuard（修复快捷键误触发）
│     └─ Sidebar.vue               # 绑定 sidebarOpacity（修复侧栏透明度）
├─ tests/
│  ├─ unit/                        # 纯逻辑单测
│  │  ├─ workbookState.test.mjs
│  │  ├─ storageErrors.test.mjs
│  │  ├─ shortcutGuard.test.mjs
│  │  └─ parseEmmx.test.mjs
│  └─ regression/                  # 代码契约回归（轻量文本断言，确认修复已落地）
│     └─ template-bindings.test.mjs
└─ package.json                    # "test": "node --test"

electron-app/
├─ main.js                         # 接入 install-meta（修复快捷方式 exe 名）
├─ install-meta.js                 # 新增：安装器 exe 名（被测 + 接入 main.js）
├─ package.json                    # build.files 已排除 tests/** 与 *.test.*
└─ tests/
   └─ install-meta.test.mjs
```

---

## 3. 运行方式

```bash
# 前端（含 unit + regression，自动发现所有 *.test.mjs）
cd web && npm test
# 或等价： node --test

# 主进程
cd electron-app && node --test tests
```

> 注：项目使用 Node v22，`node --test` 无参即递归发现 `**/*.test.mjs`，跨平台（bash/cmd/pwsh）均可用。

---

## 4. 测试不参与打包（关键约束）

三层保障，确保 `Setup.exe` 不含测试代码：

1. **前端构建只产物不源码**：`web` 构建产物在 `web/dist`，由 `copy.js` 拷入 `electron-app` 资源；`web/src`、`web/tests` 源码从不进入打包图。
2. **electron-builder 显式排除**（`electron-app/package.json` → `build.files`）：
   ```json
   "!tests/**", "!test/**", "!**/tests/**", "!**/*.test.*", "!**/*.spec.*"
   ```
3. **依赖无关**：测试仅在 `node --test` 时被消费，不作为任何运行时 `require/import` 的副作用存在。

> 验证建议（可选，较重）：`cd electron-app && npx electron-builder --win --x64 --dir` 仅打包不生成安装包，随后检查 `dist-electron/*/resources/app` 下不应出现 `tests/` 与 `*.test.*`。

---

## 5. 功能点 → 测试用例映射

| 功能点（细化见《功能点细化》） | 测试文件 | 覆盖 |
|---|---|---|
| 多文件状态机：隔离/切换/重命名/去重/另存为/持久化 | `workbookState.test.mjs` | 9 例 |
| .emmx 导入：bin 过滤（含嵌套目录）/ 损坏文件兜底 | `parseEmmx.test.mjs` | 6 例 |
| 快捷键焦点守卫 | `shortcutGuard.test.mjs` | 7 例 |
| 存储配额异常判定 | `storageErrors.test.mjs` | 4 例 |
| 安装器 exe 名与 productName 一致 | `install-meta.test.mjs` | 2 例 |
| 修复落地契约（Sidebar/Edit/api/main） | `template-bindings.test.mjs` | 4 例 |

---

## 6. Bug → 回归用例映射

| Bug（严重度） | 来源 | 回归用例 |
|---|---|---|
| 主进程快捷方式指向错误 exe（高） | 模块9 | `install-meta.test.mjs` + `template-bindings` 断言 `getAppExeName(app.getName())` |
| 状态机数据双重嵌套（中） | 模块1 | 由 `workbookState.test.mjs` 的隔离/持久化用例守护（Edit.vue 调用点需 code review） |
| 标题栏遮挡画布顶部 34px（中） | 模块2 | **手工验收**（见 §7） |
| .emmx bin 正则漏嵌套目录（中） | 模块4 | `parseEmmx.test.mjs::isPageBinEntry` 嵌套路径 |
| 侧栏透明度不生效（中） | 模块5 | `template-bindings` 断言 Sidebar.vue 绑定 `sidebarOpacity` |
| 输入框聚焦误触发快捷键（中） | 模块7 | `shortcutGuard.test.mjs` + `template-bindings` 断言 Edit.vue 接入 |
| 存储异常恒真误报（低） | 模块1 | `storageErrors.test.mjs` + `template-bindings` 断言移除 `if ('exceeded')` |
| .emmx 长文本误删（低/中） | 模块4 | **手工验收**（解析需 DOM，纯 node 难构造长文本样本） |
| 背景图加载失败无反馈（低） | 模块5 | **手工验收** |
| macos.less 死变量（低） | 模块8 | **静态检查**（eslint/人工） |

---

## 7. 需手工验收的 UI / CSS 类项

自动化难以覆盖的视觉/交互项，建议纳入发版前 Manual Checklist：

- [ ] 标题栏（34px）不遮挡画布顶部，画布顶部 34px 可点击/框选。
- [ ] 调「左侧工具栏透明度」时主侧栏面板跟随变化（非仅触发按钮）。
- [ ] 节点备注：选语言插入代码块在任一编辑器模式下正确高亮；WYSIWYG 模式插入不丢内容。
- [ ] .emmx 含长文本话题（>1000 字）不被静默丢弃。
- [ ] 画布背景设为失效图片 URL 时回退纯色且不崩。
- [ ] 安装包安装后桌面/开始菜单快捷方式可正常打开应用（验证 `思绪思维导图.exe`）。
- [ ] 双击 `.smm` 可用本程序打开（若需求含文件关联）。

---

## 8. 本次顺带修复的 Bug（测试驱动）

为让上述 bug 可测，将耦合在组件/主进程中的逻辑抽成纯函数并修复：

1. `parseEmmx.js`：`isPageBinEntry()` 抽出 + 正则修正为 `/(^|\/)page.*\.bin$/i` → 嵌套目录 import 不再残缺。
2. `api/index.js`：用 `isQuotaExceededError()` 取代 `if ('exceeded')` 恒真 → 不再误报存储超限。
3. `Edit.vue`：接入 `shouldFireGlobalShortcut(document.activeElement)` → 输入框中不误触发 F2/Ctrl+O。
4. `Sidebar.vue`：`:style` 绑定 `sidebarOpacity` + `mapState` 取值 → 侧栏透明度生效。
5. `main.js`：`APP_EXE = getAppExeName(app.getName())` → 快捷方式指向 `思绪思维导图.exe`（修高危）。

---

## 9. 新增 / 修改文件清单

**新增**
- `web/src/api/storageErrors.js`
- `web/src/utils/shortcutGuard.js`
- `electron-app/install-meta.js`
- `web/tests/unit/{workbookState,storageErrors,shortcutGuard,parseEmmx}.test.mjs`
- `web/tests/regression/template-bindings.test.mjs`
- `electron-app/tests/install-meta.test.mjs`
- `测试框架设计.md`

**修改**
- `web/src/utils/parseEmmx.js`（抽 `isPageBinEntry` + 修正则）
- `web/src/api/index.js`（接入 `isQuotaExceededError`）
- `web/src/pages/Edit/components/Edit.vue`（接入 `shortcutGuard`）
- `web/src/pages/Edit/components/Sidebar.vue`（绑定 `sidebarOpacity`）
- `electron-app/main.js`（`APP_EXE` 改用 `getAppExeName`）
- `electron-app/package.json`（`build.files` 排除测试）
- `web/package.json`（新增 `test` 脚本）
```
