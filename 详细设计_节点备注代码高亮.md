# 详细设计：节点备注代码高亮（NodeNote Code Highlighting）

> 版本：v1.0.17（对应未提交改动 `web/src/pages/Edit/components/NodeNote.vue` +65 行）
> 作者：CodeBuddy
> 日期：2026-08-29
> 模块归属：思绪思维导图（Electron + Vue2 + simple-mind-map + ToastUI Editor）

---

## 1. 功能概述

在原「节点备注」能力基础上，把纯文本备注升级为**支持 Markdown + 代码块语法高亮的富文本编辑器**。用户在节点备注中书写 ` ```python ` 等代码块后，预览区实时以 Prism 主题着色，满足「技术脑图」场景下记录代码片段、配置、SQL 等需求。

### 1.1 用户可见能力
- 弹窗内嵌 Markdown 编辑器（ToastUI Editor），左右分栏：左编辑 / 右预览。
- 预览区代码块按语言语法高亮（16 种语言）。
- 编辑器下方「语言」下拉 + 「插入代码块」按钮，一键在光标处插入 ` ```lang ` 骨架。
- 备注内容随节点保存（`.smm` 文件内 `note` 字段），重开自动恢复。

---

## 2. 设计目标 / 非目标

| 目标 | 说明 |
|---|---|
| 兼容现有备注存储 | 复用 `node.getData('note')` / `node.setNote()`，不新增数据契约 |
| 低侵入 | 仅改动 `NodeNote.vue`，不触碰画布 / 主进程 |
| 多语言高亮 | 覆盖主流语言（JS/TS/Py/Java/C/C++/C#/Go/Rust/CSS/JSON/Bash/SQL/YAML/MD/XML） |

| 非目标（本期不做） | 原因 |
|---|---|
| 深色主题跟随 | 见 §8 风险项 6，后续优化 |
| 毛玻璃视觉统一 | 设计独立，不在本特性范围 |
| 代码块行号 / 复制按钮 | 超出最小可用范围 |

---

## 3. 整体架构

```
                  ┌─────────────────────────────────────────────┐
  右键菜单/工具栏  │            NodeNote.vue (el-dialog)          │
  "备注" 事件 ───▶ │  ┌─────────────── ToastUI Editor ─────────┐ │
                  │  │  Markdown 编辑区 │  实时预览区(Prism高亮) │ │
                  │  └───────────────┬─────────────────────────┘ │
                  │        noteCodeBar: [语言▾][插入代码块][tip]   │
                  └───────────────────┬───────────────────────────┘
                                      │ editor.getMarkdown()
                                      ▼
                            node.setNote(markdownString)
                                      │
                          simple-mind-map 节点数据 (note 字段)
                                      │ 保存
                                  .smm 文件
```

### 3.1 关键依赖
- `@toast-ui/editor@3.2.2`：Markdown WYSIWYG 编辑器。
- `@toast-ui/editor-plugin-code-syntax-highlight`：代码块语法解析插件（仅解析 token）。
- `prismjs`：实际着色引擎，需显式 import 语言包 + 主题 CSS。

> 关键认知：ToastUI 插件**只做语法解析**，颜色由 Prism 主题 CSS 决定。漏引 `prism.css` 会导致"无高亮"（本项目 v1.0.16 已补）。

---

## 4. 关键文件与职责

| 文件 | 职责 |
|---|---|
| `web/src/pages/Edit/components/NodeNote.vue` | 备注弹窗 UI + 编辑器生命周期 + 插入逻辑 |
| `package.json`(web) | 已含 toast-ui / prismjs / 插件依赖 |
| `make_installer.nsi` / `electron-app/package.json` | 版本号 bump 至 1.0.17（本特性随包） |

---

## 5. 数据模型与存储

备注内容以**纯 Markdown 字符串**形式存储，不区分"普通文字"与"代码块"——二者都是 MD 语法。

```ts
// 读取
const note: string = node.getData('note') || ''   // NodeNote.vue:119 / :129
// 写入
node.setNote(this.editor.getMarkdown())            // NodeNote.vue:176 / :179
```

- 数据契约不变，向后兼容旧版纯文本备注（旧文本被 ToastUI 当 Markdown 渲染，普通文字无副作用）。
- 存储位置：节点 `note` 字段 → 随 `.smm` 持久化（非全局 `localConfig`）。

---

## 6. 详细流程

### 6.1 打开备注（showNodeNote）
```
handleShowNodeNote(node?)
  → emit('startTextEdit')            // 通知画布停止文本编辑态
  → if node: appointNode=node; note=node.getData('note')
  → dialogVisible=true
  → $nextTick → initEditor()
initEditor()
  → if !editor: new Editor({el, height:500, initialEditType:'markdown',
                            previewStyle:'vertical', plugins:[[codeSyntaxHighlight,{highlighter:Prism}]]})
  → editor.setMarkdown(this.note)
```
- 编辑器**单例**：`if(!this.editor)` 保证只创建一次，后续打开仅 `setMarkdown` 重置内容。
- `appointNode` 标记"针对特定节点打开"，用于右键单节点备注场景。

### 6.2 插入代码块（insertCodeBlock）
```
insertCodeBlock()
  → lang = this.codeLang || 'text'
  → snippet = '```'+lang+'\n\n```'
  → md = this.editor.mdEditor
  → if md?.replaceSelection: md.replaceSelection(snippet)   // 光标处插入
  → else: setMarkdown(现有 + snippet)                       // 兜底追加
```
- 优先用 `mdEditor.replaceSelection`（CodeMirror API，ToastUI 3.2.2 已验证存在）。
- 兜底分支处理编辑器实例异常，避免插入失败。

### 6.3 确认 / 取消
```
confirm()
  → this.note = editor.getMarkdown()
  → if appointNode: appointNode.setNote(note)        // 单节点
  → else: activeNodes.forEach(n => n.setNote(note))  // 多节点批量
  → cancel()
cancel()
  → dialogVisible=false
  → if appointNode: appointNode=null; updateNoteInfo()
```
- 关闭时 `watch(dialogVisible)` 触发 `emit('endTextEdit')`，恢复画布编辑态。

---

## 7. UI / 交互规范

| 元素 | 样式 | 备注 |
|---|---|---|
| 弹窗容器 | `el-dialog` 宽 50%（桌面）/ 90%（移动） | `top:15vh` / `20px` |
| 编辑器 | ToastUI 默认浅色，`height:500px`，`previewStyle:'vertical'` | 左右分栏 |
| 代码工具条 `.noteCodeBar` | flex 行，gap 8px，仅桌面（`v-if=!isMobile`） | 含语言下拉 + 主按钮 + 提示 |
| 语言下拉 `.codeLangSelect` | 白底，圆角 4px，边框 `#dcdfe6`，高 28px | 默认 `python` |
| 提示 `.tip` | 字号 12px，色 `#909399` | 灰 |
| 底部按钮 | 取消（普通）/ 确定（primary） | Element 默认 |

> 高清样式见 §8 风险项 6 的统一改造建议。

---

## 8. 已知问题与风险（Bug 清单）

> 详见会话审查结论。下列项建议在下一迭代修复。

1. **【中低】WYSIWYG 模式下插入错位**
   `insertCodeBlock` 固定写 `mdEditor`，用户切到 WYSIWYG 标签时插入内容进入隐藏 MD 源码、预览不刷新。
   *修复方向*：用 `this.editor.getEditor()` 取活动编辑器；WYSIWYG 下改用命令 `editor.exec('codeBlock')` 或 `insertText`。

2. **【低，被 modal 缓解】编辑中 `node_active` 改写目标**
   工具栏「备注」入口（`appointNode=null`）下，若激活节点变化，确认写入错误节点。当前 `el-dialog` 默认 modal 挡住画布点击，难触发。
   *修复方向*：打开时快照目标节点数组，确认时固定写入快照，不依赖实时 `activeNodes`。

3. **【极低，无丢字】`this.note` 与编辑器内容脱节**
   `updateNoteInfo` 在 `node_active` 时改 `this.note`，但编辑器 DOM 仍是用户输入；因 confirm 用 `getMarkdown()` 不丢字，但字段语义混乱。
   *修复方向*：编辑期间移除 `node_active` 监听；确认一律以 `getMarkdown()` 为准。

4. **【隐患，当前可用】`xml` 依赖 markup 别名**
   `codeLangs` 的 `xml` 实际靠 `prism-markup` 别名着色，显式导入的 `prism-xml-doc`（XML 文档专用）对普通 ```xml 无作用，属冗余误导导入。
   *修复方向*：删除 `prism-xml-doc` 导入，或选项值改 `'markup'` 并加注释。

5. **【UX】插入后光标未进代码块**
   插入 ` ```python\n\n``` ` 后光标在块外。
   *修复方向*：`replaceSelection` 后用 CodeMirror `setSelection` 将光标置于两 fence 之间。

6. **【一致性，非逻辑】视觉未融入毛玻璃 / 深色**
   弹窗仍是默认白底，Prism 浅色主题在深色画布下刺眼。
   *修复方向*：弹窗加 glass 样式；Prism 主题随明暗切换（浅 `prism.css` / 深 `prism-tomorrow.css`）。

7. **【清理】死代码**
   注释掉的 `<el-input>`、`<div class="tip">换行…</div>`，及 `.tip` 的 `#dcdfe6` 规则无对应元素。

---

## 9. 后续优化路线

1. 修复 §8 全部项，尤其 #1/#2 确保多模式 / 多节点安全。
2. 接入项目级明暗主题：Prism 主题动态切换 + 弹窗 glass 化。
3. 代码块增强：行号、右上角「复制」按钮（基于 ToastUI 插件二次定制）。
4. 语言下拉支持搜索（当前 16 项线性列表，未来扩展需 `el-select` 过滤）。
5. 备注内容搜索 / 全局检索（跨节点汇总 note 文本）。

---

## 10. 验收标准

- [ ] 打开任意节点备注，编辑器正常渲染既有内容。
- [ ] 输入 ` ```python \n print(1) \n``` `，预览区关键字彩色高亮。
- [ ] 选 `java` 点「插入代码块」，光标处出现 ` ```java ` 骨架。
- [ ] Markdown 文字、列表、标题在预览区正确排版。
- [ ] 确认后关闭再打开，内容一致（持久化正确）。
- [ ] 右键单节点备注只改该节点；工具栏备注改当前激活节点。
- [ ] 移动端不显示代码工具条（dialog 宽度 90%）。
