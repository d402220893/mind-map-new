# 功能点细化 与 代码架构（补充篇）

> 配套文档：《详细设计_全量改造.md》
> 作者：CodeBuddy ｜ 日期：2026-08-29
> 目的：把 8 大功能逐层拆成最小功能点，并给出完整代码架构视图。

---

## A. 功能点细化（最小功能清单）

### A.1 多文件工作区 + 多 Sheet
- **新建文件**：顶部「＋」生成真正空白文件（不克隆当前）。
- **打开文件**：打开对话框选择 `.smm` / `.emmx`，创建新标签而非覆盖当前。
- **文件标签栏（FileTabs）**：
  - 多标签横向排列、当前激活高亮
  - 未保存红点 `●` 标记
  - 双击标签原地重命名
  - 关闭按钮（关闭前智能提示：仅未落盘文件提示"内容将丢失"）
  - 防重复打开（同路径只一个标签）
- **工作表栏（SheetTabs）**：
  - 底部多 sheet 切换
  - 新增 / 删除 / 重命名 sheet
  - 切 sheet 保留画布状态
- **状态机隔离（workbookState.js）**：
  - 每个 workbook 独立 sheetState，杜绝跨文件数据串
  - 切文件顺序：`before-workbook-switch`(先存) → `switchWorkbook` → `workbook-switched`(再载)
  - 另存为：刷新标签名/路径（`applySaveAs` + `workbook-list-changed`）
  - 重命名：同步更新 `filePath` 并 `fs.renameSync` 真实移动磁盘文件
  - 关闭最后一个自动建空白
- **保存安全**：`doSave` 用 `getCurrentFilePath()`（非 stale 的 `this.currentFilePath`），打开/拖拽的绝对路径文件 `Ctrl+S` 直接覆盖原文件。

### A.2 自定义标题栏
- 去原生菜单栏（`Menu.setApplicationMenu(null)`）。
- 自定义标题栏（即 FileTabs 顶部 34px）：
  - 窗口拖拽区（`-webkit-app-region: drag`）
  - 最小化 / 最大化(切换) / 关闭 三按钮（IPC → `smmApi.windowControls`）
  - 双击空白区最大化
  - 文件名进标题（格式 `思绪思维导图 - 文件名`）
  - 窗口 resize 后刷新最大化图标态

### A.3 节点备注代码高亮
- 备注入口：右键菜单「备注」/ 工具栏「备注」按钮。
- 弹窗内 ToastUI Markdown 编辑器（左右分栏编辑/预览，高 500px）。
- 代码块语法高亮：预览区按语言着色（16 语言，Prism 主题）。
- 「语言」下拉 + 「插入代码块」按钮（光标处插入 ` ```lang ` 骨架）。
- 备注内容随节点保存（`note` 字段，随 `.smm` 持久化）。
- 备注节点显示 💬 图标标记。

### A.4 .emmx 导入（亿图脑图 / MindMaster 逆向）
- 导入入口合并进 `Import.vue` 的 `el-upload`（与 XMind 等并列）。
- 解析策略：
  - 优先 `document.xml` 标准 XML 解析
  - 否则遍历 `mmpage/page*.bin` 私有二进制，靠「父指针 + 子列表」还原任意深度层级
  - varint 解析 + UTF-8 替换字符(`\ufffd`)校验过滤误匹配
  - 防环 / 可达性检查 / 占位页检测
  - 多顶层取最大子树为主根
  - 缺 page.bin / 解析失败 → 兜底提示页（不白屏）
- 异常处理：`try/catch` 包裹，损坏文件（非 zip）由 JSZip 抛错被捕获 → 弹错误提示。

### A.5 画布背景设置
- 设置面板提供：10 个预设纯色 + 自定义取色器 + 图片上传。
- 应用方式：写 `mindMap.el` 内联 style（`!important`）。
- 渲染兜底：`node_tree_render_end` 事件重应用，修复"改节点后背景变回默认"。
- 持久化：`setLocalConfig({canvasBackground})` → `localConfig`，**不写 .smm**（切/开不同文件不串）。

### A.6 透明度可调
- 四个滑块：工具栏 / 侧栏 / 底部导航 / 文件栏 透明度。
- 实时应用：绑定到对应容器 style。
- 持久化：`updateLocalConfig` → vuex 订阅 `storeLocalConfig` → localStorage，重启恢复。

### A.7 快捷键
- `Ctrl/Cmd+S`：保存（阻止浏览器"保存网页"）。
- `Ctrl+Shift+S`：另存为。
- `Ctrl+O`：打开文件。
- `F2`：编辑当前激活节点。
- 注册/解绑：`mounted` 注册 `window keydown`，`beforeDestroy` 解绑（修复"声明但未注册"导致失效）。

### A.8 macOS 毛玻璃视觉
- 全局设计系统 `macos.less`：设计令牌（色板/圆角/阴影/模糊）+ Element UI 组件覆盖（按钮/输入/下拉/弹窗/消息/滑块/表格）。
- 浅色 `:root` 与深色 `body.isDark` 双主题。
- 毛玻璃写法：`backdrop-filter` + 半透明 `background-color`（侧栏/右键菜单/工具栏统一 glassmorphism）。
- 菜单栏文字加深加粗，提升高透明度下可读性。
- 顶部/底部去"黑框"：FileTabs/SheetTabs 改透明背景，画布延伸到窗口边缘。

### A.9 安装包（NSIS）
- 从 WinRAR SFX 切换到 electron-builder NSIS 标准向导。
- 内置安装器（`main.js` `--install`）：拷贝程序、装 VC++ 运行库、建桌面/开始菜单快捷方式、写卸载注册表、关 server、可选启动。
- NSIS 脚本：自动卸载旧版、拷贝 `win-unpacked`、卸载注册表、快捷方式。
- 运行时补齐：`icudtl.dat` 等 Electron 运行时文件（"缺文件打不开"已解决）。

---

## B. 代码架构

### B.1 分层架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        操作系统 / 用户                             │
└───────────────┬───────────────────────────────────────┬──────────┘
                │ 双击 .smm / 快捷方式                    │ 窗口交互
                ▼                                        ▼
┌───────────────────────────────────┐   ┌──────────────────────────────────┐
│  主进程 (electron-app/main.js)      │   │  预加载 (electron-app/preload.js)  │
│  - 窗口创建 frame:false             │◀──│  - contextBridge 暴露 smmApi       │
│  - 静态服务 tryListen(51888~51898)  │──▶│    (windowControls / 文件 IO / IPC) │
│  - 安装模式 win.loadFile(install)   │   │                                  │
│  - IPC: save/rename/open/import     │   └──────────────────────────────────┘
│  - doInstall 拷贝+快捷方式+卸载      │
└───────────────────────────────────┘
                │ loadURL(file:// / http://ACTVIE_PORT)
                ▼
┌─────────────────────────────────────────────────────────────────┐
│                     渲染进程 (web/src, Vue2)                        │
│  main.js → App.vue → router.js → pages/Edit/Index.vue              │
│   ├─ store.js (vuex: localConfig 主题/透明度/背景持久化)           │
│   ├─ i18n.js / lang (4 语言)                                      │
│   ├─ pages/Edit/Edit.vue (画布宿主 + 快捷键 + 背景应用)            │
│   ├─ pages/Edit/Index.vue (多文件编排: FileTabs/SheetTabs 事件)    │
│   ├─ pages/Edit/components/* (40+ 业务组件)                        │
│   ├─ api/ (workbookState 状态机 + index 存储委托)                  │
│   ├─ utils/ (parseEmmx 逆向 / prismSetup 高亮初始化)               │
│   └─ styles/macos.less (全局毛玻璃设计系统)                        │
│                                                                   │
│   第三方：simple-mind-map(画布引擎) / @toast-ui/editor /           │
│           prismjs / element-ui                                     │
└─────────────────────────────────────────────────────────────────┘
                │ 存储
                ▼
        localStorage(localConfig)  +  .smm 文件(节点数据/备注)
```

### B.2 目录结构与职责（实际）

```
mind-map-main/
├─ web/                              # 前端源码（Vue2 + simple-mind-map）
│  ├─ src/
│  │  ├─ main.js                     # 渲染进程入口，挂载 Vue / store / i18n
│  │  ├─ App.vue                     # 根组件，@import styles/macos.less
│  │  ├─ router.js / store.js       # 路由 / 全局状态(localConfig)
│  │  ├─ i18n.js / lang/             # 国际化（4 语言）
│  │  ├─ config/ style/              # 配置 / 基础样式
│  │  ├─ pages/Edit/
│  │  │  ├─ Index.vue                # 多文件编排：组装 FileTabs/SheetTabs/Edit
│  │  │  └─ components/              # 40+ 组件，关键如下：
│  │  │     ├─ Edit.vue              # 画布宿主：初始化 mindMap、快捷键、背景应用
│  │  │     ├─ FileTabs.vue          # 自定义标题栏 + 文件标签 + 窗口按钮
│  │  │     ├─ SheetTabs.vue         # 底部工作表切换
│  │  │     ├─ NodeNote.vue          # 节点备注（ToastUI 编辑器 + 代码高亮）
│  │  │     ├─ Import.vue            # 导入入口（含 .emmx handleEmmx）
│  │  │     ├─ Setting.vue           # 设置面板（背景/透明度 UI）
│  │  │     ├─ Sidebar.vue / SidebarTrigger.vue # 左侧栏 + 触发按钮
│  │  │     ├─ Toolbar.vue / NavigatorToolbar.vue
│  │  │     └─ Contextmenu.vue / NodeIcon*.vue / NodeTag*.vue ...
│  │  ├─ api/
│  │  │  ├─ workbookState.js         # 多文件状态机（核心重构）
│  │  │  ├─ workbookState.test.mjs  # 状态机单测（10 项）
│  │  │  └─ index.js                 # 存储委托 + localStorage 读写
│  │  ├─ utils/
│  │  │  ├─ parseEmmx.js             # 亿图脑图 .emmx 二进制逆向解析
│  │  │  ├─ prismSetup.js            # Prism 语言包/主题初始化
│  │  │  └─ export*/ai.js/handleClipboardText.js ...
│  │  └─ styles/macos.less           # 全局毛玻璃设计系统
│  └─ node_modules/                  # prismjs / @toast-ui/editor / simple-mind-map
│
├─ electron-app/                     # 主进程 + 打包
│  ├─ main.js                        # 窗口/IPC/安装器/端口回退
│  ├─ preload.js                     # contextBridge 暴露 smmApi
│  ├─ install.html / index.html     # 安装向导 / 应用壳
│  ├─ make_installer.nsi             # NSIS 安装脚本
│  ├─ package.json                   # productName:"思绪思维导图" / version:1.0.17
│  ├─ bump_version.js                # 版本号自增脚本
│  ├─ dist-electron/                 # electron-builder 产物（Setup.exe）
│  └─ dist/                          # 前端构建产物（被 copy 进 app/resources）
│
├─ HANDOVER.md                       # 交接文档（任务状态/根因记录）
├─ 详细设计_全量改造.md               # 本文档配套设计
└─ 功能点细化与代码架构.md            # 本文件
```

### B.3 关键模块依赖关系

```
            ┌──────────────┐
            │  Index.vue   │  多文件编排
            └──────┬───────┘
       ┌───────────┼────────────┐
       ▼           ▼            ▼
  FileTabs    SheetTabs     Edit.vue
       │           │            │
       │           │            ├── onGlobalKeydown → doSave/openWorkbook
       │           │            ├── applyStoredCanvasBackground → localConfig
       │           │            └── 承载 simple-mind-map 实例
       ▼           ▼
  workbookState.js ◀── api/index.js（存储委托）
       │
       ▼
  localStorage + .smm 文件
       
  NodeNote.vue ──▶ @toast-ui/editor + prismSetup.js(Prism)
  Import.vue ────▶ utils/parseEmmx.js ──▶ 节点数据注入 simple-mind-map
  Setting.vue ───▶ store.js(localConfig) ──▶ Edit.vue/Sidebar/Toolbar 读取
```

### B.4 数据流与事件总线
- **全局事件总线 `$bus`**：`node_active`(激活节点变化)、`showNodeNote`(开备注)、`startTextEdit`/`endTextEdit`(画布文本编辑态)、`workbook-switched`、`workbook-list-changed`、`node_tree_render_end`(背景兜底)。
- **存储流向**：UI 操作 → `workbookState`/`api/index` → `storeData` → `localStorage(SIMPLE_MIND_MAP_DATA)` + `.smm`。
- **配置流向**：`Setting.vue` → `updateLocalConfig` → vuex `localConfig` → 各组件 `mapState` 读取 + `storeLocalConfig` 落盘。

### B.5 构建与打包链路
1. `web/` 前端 `npm run build` → 产物到 `electron-app/dist/`（即 `index.html` + `dist/js` + `dist/css`）。
2. 主进程 `electron-app/main.js` + `preload.js` 作为 `app` 入口；`electron-app/dist` 被 copy 进 `resources/app/`。
3. `electron-builder`（NSIS）打包 `win-unpacked` → `dist-electron/思绪思维导图 Setup.exe`。
4. `make_installer.nsi` 提供备用 NSIS 向导；`bump_version.js` 维护版本号。
5. 安装器 `doInstall`（`main.js --install`）：拷贝 `win-unpacked` → 建快捷方式 → 写卸载 → 关 server。

---

## C. 与《详细设计_全量改造.md》的对应
- 功能点细化（A 节）= 各模块「实现摘要」的展开版，逐层拆到最小可测单元。
- 代码架构（B 节）= 原文档缺失的全局视图，补全分层 / 目录 / 依赖 / 数据流 / 构建链。
- Bug 清单仍以《详细设计_全量改造.md》为准（本文不重复，避免漂移）。
```
